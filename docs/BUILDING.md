# Building simple-chatsrv

You need Python 3.8.2+ to build this project.

## Dependency

Debian: `python3`, `python3-venv`, `python3-pip`, `make`

## Build

Run `make venv` first, and then run `make package`, you will see output in folder `dist/`.